<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2 text-center">
      <div class="display-t js-fullheight">
        <div class="display-tc js-fullheight animate-box" data-animate-effect="fadeIn">
          <div class="row recruit-login">
            <h3 class="title-form">【ゲスト　専用ログインフォーム】</h3>
            <h4 class="description-form">下記の「専用ログイン」ボタンを押していただければ、<br>すぐにゲスト専用アカウントでログインが可能です。</h4>
            <div class="col-md-6 col-md-offset-3">
              <%= form_for(:session, url: login_path, local: true) do |f| %>
                <div class = 'form-group'>
                  <%= f.label 'メールアドレス' %>
                  <%= f.email_field :email, class: 'form-control', id: 'recruit_user_email', value: 'guest@example.com' %>
                </div>
                <div class = 'form-group'>
                  <%= f.label 'パスワード' %>
                  <%= f.password_field :password, class: 'form-control', id: 'recruit_user_password', value: 'guest1' %>
                </div>
                <%= f.submit '専用ログイン', class: 'btn btn-primary' %>
              <% end %>
              <div class="recruit-account">
                <p>＜ゲスト　専用アカウント＞</p>
                <p>メールアドレス：guest@example.com</p>
                <p>パスワード：guest1</p>
                <p>※フォームに予め入力済</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>